# Projekt-PP5

Ceneo product opinions extractor — a small Flask backend that scrapes product reviews

## Quick setup
### You will need just and uv installed on your system
Install project tooling and dependencies:

```
cd api
just setup
```

## Run (development)

Start the backend:

```
cd api
just run
```

Swagger UI (API docs) will be available at:

```
http://localhost:5000/apidocs/
```

